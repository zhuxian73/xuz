services:
  xuz-agent:
    build: .
    container_name: xuz-agent
    ports:
      - "8715:8715"
    volumes:
      - ./src/agent/memory:/app/src/agent/memory
    env_file:
      - .env
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
      - GRADIO_SERVER_PORT=8715
    restart: always
